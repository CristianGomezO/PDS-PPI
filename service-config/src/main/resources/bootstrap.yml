
spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: file:///K:/Profundizacion de desarrollo/config-data
  security:
    user:
      name: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}
server:
  port: 8000
